//renderPipe = (...fns) => (arg) => fns.reduce((value, fn) => fn(value), arg);
export class ShwimplePageBuilder {
    // static buildPage(title?: string): IPage {
    //     return new Page().createBoilerplatePage(title);
    // }
    // static buildEmptyPage(title?: string): IPage {
    //     return new Page().createEmptyPage(title);
    // }
}
